# Palate — environment variables
#
# Copy to .env.local and fill in. Without a key, Palate runs in demo mode (returns mock data).

# Anthropic API key — https://console.anthropic.com/
ANTHROPIC_API_KEY=
